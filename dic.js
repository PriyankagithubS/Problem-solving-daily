const readline=require('readline');
const inp=readline.createInterface({
    input:process.stdin
});
const userInput=[];
inp.on("line",(data)=>{
    userInput.push(data);
});
inp.on("close",()=>{
function removeDup(arr,s){
    const sSet=new Set(s);
for(let start=0;start<arr.length;start++){
    if(!arr[start]){
    
   }
}
}
 let arr=userInput[0];
 let s=userInput[1];
 console.log(removeDup(arr,s))

});
